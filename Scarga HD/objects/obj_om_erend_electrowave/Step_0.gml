
            var target;
                target = point_direction(x, y, obj_blatt.x + 8, obj_blatt.y - 8);

            motion_add(target, min(0.8, 0.6, 0.4));
            
            if (!alarm[1]){alarm[1] = 60;}
        

